{
 // "functions": {
   // "predeploy": "npm --prefix functions install"
  //},
  //"database": {
    //"rules": "database.rules.json"
  //}, 
  "hosting": {
    "public": "build",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
